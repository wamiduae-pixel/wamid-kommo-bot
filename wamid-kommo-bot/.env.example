# === Kommo base URL (your subdomain) ===
KOMMO_BASE_URL=https://YOUR_SUBDOMAIN.kommo.com

# === Integration credentials (from Kommo Settings → API & Integrations) ===
KOMMO_CLIENT_ID=YOUR_CLIENT_ID
KOMMO_CLIENT_SECRET=YOUR_CLIENT_SECRET

# === OAuth redirect (must match what's set in Kommo) ===
KOMMO_REDIRECT_URI=https://yourapp.com/oauth/callback

# === Tokens (leave empty until you mint with cURL) ===
KOMMO_ACCESS_TOKEN=
KOMMO_REFRESH_TOKEN=

# === Chat channel (set after registering channel) ===
CHAT_CHANNEL_ID=
CHAT_CHANNEL_SECRET=

# === Web server port ===
PORT=3000
